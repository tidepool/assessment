require 'spec_helper'
require File.expand_path('../load_tasks', __FILE__)

describe AnalyzeDispatcher, "Analyze Dispatcher" do
  it "should sort events into modules" do
    pending "Add the test case here"        
  end
end